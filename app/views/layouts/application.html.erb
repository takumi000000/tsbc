<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "2025年度授業点検" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>


    <meta charset="UTF-8">
    <title>授業に関するアンケート</title>
    <!-- Bootstrap（CDN版）を利用する例 -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" %>
    <style>
      /* 必要に応じて追加のスタイルをここに記述できます */
      .survey-header {
        margin-top: 2rem;
        margin-bottom: 1.5rem;
      }
      .survey-ver {
        font-size: 0.8rem;
        color: #666;
        text-align: right;
        margin-top: 1rem;
      }
      .survey-header {
        margin-top: 2rem;
        margin-bottom: 1.5rem;
      }
      .question-group {
        padding: 1rem;
        border: 1px solid #ddd;
        border-radius: 5px;
        margin-bottom: 1rem;
      }
      .question-title {
        font-weight: bold;
        margin-bottom: 0.5rem;
      }
      .form-check-label {
        margin-right: 1.5rem; /* ラジオボタン同士の余白用 */
      }
          .header-title {
      margin-top: 2rem;
      margin-bottom: 1.5rem;
    }
    .subtitle {
      font-size: 1.1rem;
      margin-bottom: 1rem;
    }

    .form-check-inline {
      margin-right: 1.5rem; /* ラジオボタン同士の間隔 */
    }
    .note-text {
      font-size: 0.9rem;
      color: #555;
      margin-top: 0.25rem;
    }
    .bg-custom {
      /* 必要なら背景色を好みに合わせて変更 */
      background-color: #ffffff;
    }
    </style>
    
  </head>

  <body>
    <%= yield %>
  </body>
</html>
